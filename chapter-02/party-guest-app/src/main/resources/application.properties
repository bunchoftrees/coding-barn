server.port=8083
spring.application.name=party-guest-app

# OAuth configuration
oauth.client.id=party-guest-app
oauth.client.secret=party-secret-key

# Service URLs
auth.server.url=http://localhost:8081
shed.service.url=http://localhost:8080

# Pretty print JSON responses
spring.jackson.serialization.indent-output=true

# Logging
logging.level.com.codingbarn=INFO
